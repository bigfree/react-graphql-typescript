import React from 'react';
import { Container } from "@material-ui/core";

const HomeRoute: React.FC = (): JSX.Element => {
	return (
		<Container maxWidth="lg">
			Home
		</Container>
	)
}

export default HomeRoute;