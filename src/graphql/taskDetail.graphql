query TaskDetail(
    $taskWhereInput: TaskWhereUniqueInput!
) {
    task(
        where: $taskWhereInput
    ) {
        id
        name
        content
        userId
        user {
            name
        }
        createdAt
    }
}