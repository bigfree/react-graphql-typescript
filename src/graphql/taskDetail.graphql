query TaskDetail(
    $taskWhereInput: TaskWhereUniqueInput!
) {
    task(
        where: $taskWhereInput
    ) {
        id
        name
        content
        labels {
            name
        }
        userId
        user {
            name
        }
        createdAt
    }
}